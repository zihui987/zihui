# B5 资产归属表 — `.hermes` 脏树分类（2026-06-19）

> 范围：`C:/Users/Administrator/.hermes/`
> Git 脏树总量：分类前快照 2135 条，补充 .gitignore 后 1156 条
> 本轮范围：只分类 + `.gitignore` 边界，不大清文件

---

## 一、运行态（应进 .gitignore）

| 路径 | 大小 | 特征 | 建议 |
|------|------|------|------|
| `logs/` | 30M | 运行时日志 | 应忽略 |
| `sessions/` | 2.6M | 会话历史 | 应忽略 |
| `state.db` | 3.0M | SQLite 状态库 | ✅ 已忽略 |
| `state.db.bak` / `state.db.bak.*` | 358M | 数据库冷备 | 应补充忽略 |
| `state-snapshots/` | — | 运行时快照（untracked） | 应忽略 |
| `cron/output/` | — | cron 输出 | ✅ 已忽略 |
| `cron/jobs.json` | — | cron 状态 | 应忽略 |
| `desktop-attachments/` | 115M | 附件的运行时缓存 | ✅ 已忽略 |
| `*.pid` | — | 进程锁 | ✅ 已忽略 |
| `.gateway-planned-stop.json` | — | 网关状态 | ✅ 已忽略 |
| `channel_directory.json` | — | 运行时 | 应忽略 |
| `hermes_history` | — | 会话历史 | ✅ 已忽略 |
| `start_gateway_hidden.vbs` | — | 启动脚本（未跟踪） | 可忽略 |

## 二、缓存/临时（应进 .gitignore）

| 路径 | 大小 | 特征 | 建议 |
|------|------|------|------|
| `cache/` | 3.2M | 通用缓存 | ✅ 已忽略 |
| `image_cache/` | 812K | 图片缓存 | ✅ 已忽略 |
| `audio_cache/` | — | 音频缓存 | ✅ 已忽略 |
| `bootstrap-cache/` | — | 启动缓存 | ✅ 已忽略 |
| `tmp/` | — | 临时文件 | ✅ 已忽略 |
| `sandboxes/` | — | 沙箱 | ✅ 已忽略 |
| `__pycache__/` | — | Python 字节码 | ✅ 已忽略 |
| `node_modules/` | 22M | npm 依赖 | ✅ 已忽略 |
| `models_dev_cache.json` | 2.3M | 模型缓存（未忽略） | 应忽略 |
| `provider_models_cache.json` | — | 模型缓存（untracked） | 应忽略 |
| `ollama_cloud_models_cache.json` | — | 模型缓存（untracked） | 应忽略 |
| `.skills_prompt_snapshot.json` | — | 技能快照（untracked） | 应忽略 |
| `memory_heartbeat.json` | — | 记忆心跳（untracked） | 应忽略 |

## 三、Skills 实体改动（952 条变更，本轮不动）

统计：modified ~900+, deleted ~589, untracked ~455

这是一级目录结构顶部的集中变更区。原因推测为 hermes 启动时动态注册/刷新 skill 缓存、重写 SKILL.md 元数据，或在 skills/ 内跨目录重排。

**本轮不做清理。** 分类记录主要子目录现状：

```
skills/ 下典型子目录（数量级）：
  autonomous-ai-agents/   ~几十项
  github/                 ~几十项
  pmos-toolkit/           ~大量（pmos 工具库）
  software-development/   ~中等
  apple/                  ~少量
  wecomcli-*/             ~若干个
```

待后续决定：git reset HEAD -- skills/ → 恢复 HEAD 快照，git clean 清理 untracked。需确认是否插入了新 skill。

## 四、Memoirs 历史记忆（仅 641K，本轮不动）

目录较小且内容稳定。存储的是 hermes 运行时记忆持久化产物。不做 gitignore，保留为有版本的历史。但脏树中 memoirs 修改极少（<10 条）。

## 五、Scripts 工具资产（~10 条 untracked，本轮不动）

`scripts/` 下存放的是 hermes 可执行工具脚本（Python/Shell）。本轮不做操作。

## 六、配置/安全敏感（应进 .gitignore）

| 路径 | 状态 | 建议 |
|------|------|------|
| `auth.json` / `auth.lock` | ✅ 已忽略 | — |
| `.env` | ✅ 已忽略 | — |
| `douyin_cookie.txt` | untracked | 应在 .gitignore 中补充 |

## 七、构建/二进制产物（应进 .gitignore）

| 路径 | 大小 | 建议 |
|------|------|------|
| `hermes-setup.exe` | 7.3M | 应忽略（安装包） |
| `bin/` | 66M | 编译产物，应忽略 |
| `hermes-agent/` | 1.2G | venv 虚拟环境，应忽略 |

## 八、大文件/备份归档（补充忽略）

| 路径 | 大小 | 建议 |
|------|------|------|
| `backups/` | 299M | ✅ 已忽略 |
| `*.bak` / `*.bak.*` | — | ✅ 已忽略 |
| `pastes/` | 500K | ✅ 已忽略 |
| `profiles/`（根目录级） | — | ⚠️ 当前 .gitignore 中标记为"大文件目录"一并忽略，但 profiles/ 内含用户配置（config.yaml），**不建议整体忽略** |

---

## 本轮 .gitignore 补充项

基于上述分析，.gitignore 需补充以下条目：

```gitignore
# ── 补充：日志与会话 ──
logs/
sessions/
state.db.bak
state.db.bak.*
state-snapshots/

# ── 补充：运行时缓存文件 ──
models_dev_cache.json
provider_models_cache.json
ollama_cloud_models_cache.json
.skills_prompt_snapshot.json
memory_heartbeat.json
cron/jobs.json

# ── 补充：安全敏感 ──
douyin_cookie.txt

# ── 补充：二进制/构建产物 ──
hermes-setup.exe
bin/
hermes-agent/
```

> ⚠️ 注意：取消 `profiles/` 在大文件目录段的通配忽略，改为仅 ignore `profiles/*/weixin/`（已有此行）。

---

## 本轮不做的

| 事项 | 原因 |
|------|------|
| git reset / clean skills/ | 需确认是否有新增 skill |
| memoirs/ 清理 | 太小且稳定，不动 |
| scripts/ 清理 | 仅 10 条，不动 |
| profiles/ 整体忽略 | 含 config.yaml 用户配置 |
